overwrite: true
schema: "http://localhost:4000/schema.graphql"
documents: "src/**/*.graphql"
generates:
  schema.graphql:
    plugins:
      - schema-ast
  src/graphql/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
    config:
      withHOC: false
